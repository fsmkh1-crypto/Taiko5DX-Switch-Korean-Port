# Message coverage diagnostic 218

PC_PATCH_ORACLE_GATE=PASS for the pinned original/PC Korean TAI5MSG pair and Mapping 10,036 bytes. This experiment tests one cause family: the delivered 217 selector omitted Korean message programs. It does not issue semantic release admission.

## Diagnostic contract

The user explicitly requested continued implementation through delivery of a new patch. The target is a controlled runtime diagnostic, not R1/R2/R3 release approval. No existing semantic gate is relaxed: unknown all-caller, grammar, layout and runtime results remain open, and no INCLUDE_KO ledger is produced. The release application adapter remains unchanged. The diagnostic cohort is recorded separately from its reviewed prerequisites.

The builder preserves all 3,227 reviewed baseline messages byte for byte and all 1,233 source/PC-identical messages. Among 10,372 omitted changed messages, the target-VM parser rejects 134 field/control cases. Propagating those holds over explicit stored CALL/JUMP edges rejects another 18. The remaining 10,220 diagnostic targets use exact PC replacement bytes, without guessed text, names, operands, pointer adjustments or grammar rewrites. Layout bytes may follow the PC payload where control structure is identical. All 152 held targets retain baseline bytes and are enumerated in the generated ledger.

This closure covers explicit TAI5MSG C/J edges only. It is not proof that EVENT/native callers are semantically closed. Thirteen EVENT files, the font and the five-record IPS are inherited byte-for-byte from diagnostic 217. The other 156 EVENT requests are not included. No TE5, SNR or identity-name resources are changed.

## Verified target and parser scope

The exact Switch 1.1.3 main was recovered by listing Drive's Switch folder directly. SHA-256: b366e692208f3c0cc18bc1884ef95689b6b11d722fa2d749b8500abccbc3109b. BuildID: D9120950C258610A746F4A31CE3A3B376DE393D9. Earlier claims that this input was unavailable were search limitations, not actual absence.

Target offsets: constructor 263ecc installs vtable 9dff70; its +18 slot is atom decoder 263848. Expression/comparison/boolean parsers are 43e1e8/43f138/43ecbc. C/J dispatch is 43e9a4; operands are little-endian global IDs, block=id//1000 and local=id%1000. Selector-property handlers rewind a <=31 lookahead, so 02 is not uniformly three bytes. Function selector C8 uses one or two nested expressions according to its table at 75cb6e. The loader adds 5B to stored block bytes; offsets in field reports are decoded message-local coordinates.

The bounded Unicorn check executes the original ARM64 atom parser: 4,821 unique corpus atom contexts matched their predicted cursor consumption. Game services are stubbed. This establishes neither actual game behavior nor a runtime PASS. Seven regression tests cover variable-width operands, numeric condition fields, assignments, C/J distinction, unknown rejection and nested expressions.

## Build and provenance

Run build.py --preflight with --original, --pc, --mapping, --baseline, --main and --output. This writes reports only. Commit the exact builder, parser and vendored dependencies before any game artifact is emitted. Authenticated source readback must produce the receipt accepted by --receipt and --commit. Run without --preflight only after that freeze.

All 33 block sizes are checked against 0x20000, and the B32 physical extent contract is preserved. After serialization, an independent loader-level decoder checks all 14,832 payloads and next offsets without calling the serializer's parser. ZIP readback checks all 17 members. These checks address transport/storage consistency, not visual layout or runtime output capacity.

The input main and all five inherited IPS original-byte guards have been checked. This does not verify the user's installed main or Eden version. No Eden execution occurs in this environment.

## Preserved unresolved scope

V369_UNCHANGED and V371_PARTIAL_BLOCKED remain. The 48 complete root contracts, historical V357 46 roots, and V371 five-root obligation chain retain separate denominators. B0:181/307, suffix3, ROOT locator contracts, inline 139, non-B24 reflow, 60 source-identity holds, TE5/SNR, legacy PASS/source-version provenance, and broader circular-readback concerns are not closed by this diagnostic. Candidate observations are not all confirmed defects.

## Runtime observation request

Replace the old diagnostic mod with 218; do not enable both. Check the same tavern hostess prompt, first meeting and invitation, then unrelated normal conversations. Japanese runtime-inserted identities remain intentional under IDENTITY_IN_PROSE_V1; PC-authored Korean prose names remain as authored. Record the exact ZIP SHA, installed game version, scene and full screenshot for any remaining mixed prose, corruption, truncation or crash.
