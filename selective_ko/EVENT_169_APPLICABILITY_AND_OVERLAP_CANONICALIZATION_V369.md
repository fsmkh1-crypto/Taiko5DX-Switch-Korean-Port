# EVENT 169 exact applicability and overlap design canonicalization — V369

Date: 2026-09-22 (KST)
Parent: `756596e57353a0ed8f3538c22b4fbeac8f08a326` (V368)
Scope: DOCUMENTATION AND EXACT ARTIFACT CANONICALIZATION ONLY

## Resume authority

1. `artifacts/event_169_v368_exact_applicability_v1/INDEX.json`
2. `artifacts/event_169_v368_exact_applicability_v1/PROVENANCE.json`
3. `artifacts/event_169_v368_exact_applicability_v1/VALIDATION.json`
4. `artifacts/event_169_overlap_cluster_design_v1/INDEX.json`
5. `artifacts/event_169_overlap_cluster_design_v1/REPORT.md`
6. `artifacts/event_169_overlap_cluster_design_v1/EXACT_CENSUS.md`

Read these bounded artifacts and exact dependencies. Do not regenerate V366 membership, redo the closed overlap analysis, or reopen V367/V368 verified findings merely to resume in another task.

## V368 applicability ledger

The exact generalized residual is `2,864 - 426 = 2,438` source owners. Every row inherits INCLUDE_KO from the V368 closure. The ledger materializes the conjunction of exact V366 binding, actual PC-KO byte locators, source/runtime ownership, and applicable cross-carrier root authority. Family labels describe the evidence and do not grant admission.

| Measure | Value |
|---|---:|
| Rows / INCLUDE_KO | 2,438 / 2,438 |
| UNRESOLVED | 0 |
| EVENT files | 106 |
| Detector-related occurrences | 2,671 |
| Duplicate owner keys / unknown surface predicates / fallback classifications | 0 / 0 / 0 |
| Nested outer delegations | 2 |
| Rows with PC-authored additional escapes, preserving source escape order | 550 |

`APPLICABILITY.jsonl` is 4,498,481 bytes. Its deterministic SHA-256 is:

```text
b9c421d18e7ce0a5d7603d7d3e764ad102bf791d965d7216edac85682a838c43
```

The canonical large artifact is [V369_V368_EXACT_APPLICABILITY_2438.zip](https://drive.google.com/file/d/1zROWsuux1d3m7T4SAKfuimHXuiJ_GJbv/view?usp=drivesdk), 490,825 bytes, SHA-256:

```text
4e8b5dc2899789541e010ec16adcf1921b05673913e6840c3b2e8eb0cfa07189
```

Verify the archive hash, then the separate uncompressed ledger hash and row count. The archive contains the original UTF-8 JSONL, provenance, validation, and the documentation-only materializer. Compact metadata is in Git. Offsets are integer byte positions; intervals are half-open; rows sort by filename then source offset; keys sort lexically; serialization uses compact JSON, LF, and a terminal LF. Hex literal bytes are authoritative; Korean surface strings are preserved as authored.

| Cause family | Rows |
|---|---:|
| Direct particle | 1,334 |
| Copula / derived / composite surface | 363 |
| Lexical 가문 | 702 |
| Lexical 가신 | 2 |
| Cross-carrier authored suffix | 14 |
| Direct + derived | 12 |
| Direct + lexical 가문 | 2 |
| Derived + lexical 가문 | 4 |
| Direct + cross-carrier suffix | 5 |

Cross-carrier records pin `%05 -> B0:73`, `%06 -> B0:82`, `%07 -> B0:91`, `%0B -> B0:139`, and `%23 -> B0:328` to the V356 target size/hash and V357 authority. The final V368 three-row closure is explicit: `EFF27C00:3BA0 -> 41F8`, `EFF27C00:3BA8 -> 4200`, and `EFF2D100:7A34 -> 8AF0`. Outer delegation retains exact inner binding, identical absolute occurrences, preserved source/target containment, zero outer-exclusive occurrences, and independently admitted inner ownership.

No unknown row is defaulted to direct particle. Explicit byte-surface predicates fail on an unknown form; all mixed components retain their own evidence and root dependencies. This ledger is policy applicability, not permission to apply existing ECF wrappers blindly or to invent new grammar transformations.

## Promoted overlap authority

The completed local REPORT, EXACT_CENSUS and INDEX are promoted with their dependency ledgers. EXACT_CENSUS and graph membership bytes are retained. REPORT is an English revision; INDEX and VALIDATION describe canonical status. Responsibility-unit rows retain their exact membership and receive final-design status wording. Original local identities are preserved in INDEX, and the replay receipt remains evidence of the earlier documentation run rather than a claim that revised metadata is byte-identical.

Frozen accounting is **210 nodes / 130 relations / 87 connected clusters / 80 final write-responsibility units**. Preserve:

- 91 product-relevant INCLUDE_KO outer rows and the 108 / 22 direct/composite relation split;
- seven relocation-sharing cluster pairs and 12 shared fields;
- 17 fixed-particle, 22 overlap-composite, and five leading-control recovery intersections;
- runtime-only inner owner `EKF00800:F60`;
- five connected NON_KOREAN_TARGET preservation-boundary owners;
- C31 canonical composite reconstruction before the approved particle transform.

The 87 components are not independent serializer transactions. `TRANSACTION_DOMAINS.jsonl` defines the 80 final write-responsibility units. Separate disjoint payload regions do not permit multiple final writers for a shared relocation field. Final file-layout ownership remains necessary.

## Validation and claim boundary

The 17 completed local artifact checks are inherited, not rerun. Promotion verifies retained bytes against the verified local INDEX, unchanged unit membership, canonical file hashes, and absence of implementation/product paths in the change set. The exact applicability ledger validates owner uniqueness, occurrence ranges and bytes, source escape order, explicit predicates, root dependencies, and V368 delegation. All newly authored technical prose is English; actual Korean literals remain Korean. Historical documents are not translated merely for consistency.

V366 source universe `66,987 = 66,956 + 31`, V367 runtime continuity PASS, and V368 post-policy `52,491 / 0 / 14,496` remain unchanged. No builder or serializer code, product EVENT or TAI5MSG bytes, build/package/IPS, or hardware/runtime package is changed or generated.

Git writes use only `create_blob -> create_tree -> create_commit -> update_ref(force=false)`. Canonicalization ends with this checkpoint. **STOP. Serializer implementation requires a fresh explicit instruction.**
