# VALIDATION LEDGER — F1 static write authorization

Date: 2026-09-12  
Scope: deterministic row-level provenance closure and static write authorization for the 158 V089 F1 static-safe candidates only. No builder, IPS, runtime artifact, game-file modification, yomi work, shared-owner closure, padding reconstruction, or other residual-family work.

Detailed report: `docs/F1_STATIC_WRITE_AUTHORIZATION.md`

## V093 — the historical F1 selector is deterministically reproducible from canonical inputs

**Status:** `VERIFIED`

Using the canonical Stage-1 report bundle and canonical Switch `main`, `tools/f1_static_authorization.py` reproduces the original F1 sequence procedure exactly:

- PC scope `R1-R3250`;
- anchor logical length >= 8 bytes;
- anchor logical value unique once in PC scope and once among the 3,803 JP localization logical entries;
- no nonzero bytes after the first PC NUL;
- `localization ID = PC record number + segment delta`;
- maximal consecutive correspondence search run >= 5 rows;
- at least two same-delta anchors;
- no candidate gap/delta-reset crossing;
- historical mapping only from first through last accepted anchor;
- V088 canonical promotion additionally requires final bounded interval >= 5 rows.

Exact replay totals:

```text
search segments                         142
anchor-bounded mapped rows            1,402
Stage-2 residual rows in those bounds   278
accepted final segments                 112
accepted mapped rows                  1,311
accepted residual rows                  262
F1_RULE_REJECTED                         16
```

The exact V088 rejected residual set also reproduces:

`R284, R286, R821, R1275, R1985, R1986, R1987, R2014, R2015, R2161, R2253, R2425, R2426, R2480, R3181, R3182`.

Historical/accepted storage splits reproduce exactly:

```text
historical full-window                  197
historical padding mismatch              81
accepted full-window                    187
accepted padding reconstruction          75
capacity/NUL failures                     4
shared-owner blockers                    25
static authorization candidates         158
```

**Reuse rule:** Future work must use this deterministic selector or the frozen manifest. Do not recreate F1 membership using raw occurrence uniqueness, pooled-object identity, or aggregate-count fitting.

## V094 — 158 F1 rows have row-level DIRECT_PORT static write authorization

**Status:** `VERIFIED / STATIC WRITE AUTHORIZED`

Canonical manifest:

- `docs/manifests/F1_STATIC_WRITE_AUTHORIZATION_MANIFEST.jsonl.gz`
- row count: `158`
- uncompressed manifest-content SHA-256: `c612bcf55d0c139d55dee42a6a6397f702ead0f1628b47b45c46512fd1b52bff`
- canonical `.jsonl.gz` SHA-256: `8bbbeb03695b4bd06f028b9c9cfe0d367af170a012af56803f3a8553356a0b68`
- ordered authorized `source_id` list SHA-256: `87d5a7531b5e2b3841fe4ba093a87dd9da11e8c85b5d4184c6ae705436c1833e`

All 158 are simultaneously:

```text
unique PC source IDs             158
unique localization IDs          158
unique physical target objects   158
unique authorization action IDs  158
language domain                  JP_ONLY
storage                          STATIC_RODATA
window/object cardinality        1:1
exact original-byte guard        PASS
terminating-NUL preservation     PASS
physical logical owners          1 each
unintended F1 overlap            0
Stage-2 overlap                  0
capacity failure                 0
shared-owner blocker             0
```

Terminator proof modes:

- existing target NUL immediately after unchanged-length write remains untouched: `124`;
- replacement contains its NUL inside the exact guarded window: `34`.

Each row receives a stable authorization action ID and the overlay:

```text
terminal_disposition_overlay = DIRECT_PORT
action_family                = F1_LOCALIZATION_STATIC_DIRECT
action_status                = STATIC_WRITE_AUTHORIZED_NOT_IMPLEMENTED
```

The manifest includes source identity, F1 segment/delta/anchor provenance, localization ID, JP source slot, runtime destination-cell identity, physical target object, write interval, guard, NUL proof, ownership, overlap state, and falsification condition.

Two consecutive replays generated byte-identical outputs:

- manifest-content SHA-256 `c612bcf55d0c139d55dee42a6a6397f702ead0f1628b47b45c46512fd1b52bff`;
- compressed manifest-file SHA-256 `8bbbeb03695b4bd06f028b9c9cfe0d367af170a012af56803f3a8553356a0b68`;
- summary SHA-256 `e77662199707ab5597d2e387fe76d5eda5fe12dc0d211bf40333e0fa6563d140`.

**Authorization boundary:** V094 authorizes these exact 158 manifest actions for a later static builder implementation if all canonical identities and per-row guards remain valid. It does not itself implement, emit, or runtime-test those actions.

The following remain outside V094:

- capacity failures: `R2674`, `R2684`, `R2712`, `R3142`;
- 75 padding-reconstruction rows;
- 25 shared-owner blocker rows;
- 16 V088-rejected residual rows.

No prior F1 exclusion or safety rule is relaxed.

## V095 — F1 Build-ID terminology / duplicate-run cleanup is documentation-only

**Status:** `VERIFIED / METADATA-ONLY`

A cleanup review found no change to F1 membership, target resolution, or static authorization. The apparent Build-ID discrepancy is representational:

- canonical project Build ID = 20-byte identity `D9120950C258610A746F4A31CE3A3B376DE393D9`;
- NSO header field at `0x40:0x60` = 32 bytes;
- the canonical `main` stores the 20-byte identity followed by 12 zero padding bytes;
- `tools/f1_static_authorization.py` compares the exact 32-byte NSO field by design;
- `F1_STATIC_WRITE_AUTHORIZATION_SUMMARY.json` schema V1 uses the legacy key `canonical_build_id` for that 32-byte serialized field. This key name is retained to avoid needless artifact churn; the project-level Build ID remains the 20-byte identity above.

The duplicate-execution concern was also checked at the canonical repository level. The canonical tree contains one F1 machine manifest, one summary, one replay tool, one report, and one F1 authorization ledger. Repeated local replay outputs used during provenance closure were not committed as duplicate canonical artifacts.

The canonical replay invariants remain unchanged:

- static-authorized rows: `158`;
- manifest-content SHA-256: `c612bcf55d0c139d55dee42a6a6397f702ead0f1628b47b45c46512fd1b52bff`;
- compressed manifest SHA-256: `8bbbeb03695b4bd06f028b9c9cfe0d367af170a012af56803f3a8553356a0b68`;
- ordered authorized `source_id` SHA-256: `87d5a7531b5e2b3841fe4ba093a87dd9da11e8c85b5d4184c6ae705436c1833e`.

No builder/IPS/runtime work and no reauthorization was performed by this cleanup.

**Reuse rule:** Treat V095 as terminology/provenance cleanup only. Do not reopen V093/V094 or regenerate the 158-row set solely because the summary V1 key stores the padded NSO field.

## Stopping point

F1 row-level provenance and static authorization closure is complete for the 158-row set.

No builder/IPS/runtime artifact was created or modified. Fresh user execution signal is required before implementation or another family analysis.
