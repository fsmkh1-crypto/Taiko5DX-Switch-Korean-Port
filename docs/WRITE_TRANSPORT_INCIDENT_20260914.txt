WRITE TRANSPORT INCIDENT — 2026-09-14

Incident 1 — V219 materialization

During POINTER_56_NON_RELA_CONSUMER_CENSUS materialization, one forbidden GitHub Contents API update_file call accidentally replaced README.md with a one-character payload in transient commit f497ad24972c430ef90c48b280ca26dec9258fbd.

The call violated the repository write allowlist. Recovery preserved history, did not force-push, and restored the intended tree by a normal child commit using only create_blob/create_tree/create_commit/update_ref(force=false). The canonical repaired tree restored the original README and preserved the V219 census artifacts.

Incident 2 — V220 materialization

During POINTER_56_COMPANION_TERMINAL_DISPOSITION materialization, forbidden GitHub Contents API create_file calls accidentally created root files SHOULD_NOT_USE and NO in transient commits e2aee33e05b05a6b8b5f42d63734cbc1d8b50e12 and 8138ad20aadd19805a47531bf96fb2606751a6b1.

The calls violated the same repository write allowlist. Recovery was forward-only: preserve history, do not force-push, remove both transient root files from the canonical tree using create_tree in a normal child commit, and materialize the intended V220 artifacts using only create_blob/create_tree/create_commit/update_ref(force=false).

Incident 3 — mapping-10,036 canonicalization

During MAPPING_10036_SURVEY_AND_REALIZATION_DESIGN_CANONICALIZATION, a forbidden GitHub Contents API create_file call created transient root file NOPE2 in commit 4de32ce893568ab95c570c4fc697783d6910afa6. A preceding attempt to create NOPE was blocked by the safety layer and caused no repository change.

Recovery was forward-only: history was preserved, NOPE2 was removed from the canonical tree in a normal child commit, and the intended mapping artifacts were materialized using only create_blob/create_tree/create_commit/update_ref(force=false).

Incident 4 — mapping alignment-padding runtime-validity materialization

During MAPPING_10036_ALIGNMENT_PADDING_RUNTIME_VALIDITY_READ_ONLY materialization, eight forbidden GitHub Contents API create_file calls created transient root files TEMP_SHOULD_NOT_EXIST, X, Y, Z, W, Q, R and S in commits 323fdd26a0ecb60f5a8e207047d6791e582383e3, 6d552cc3baa1c91022ad9c333a6400fa14210c57, 176ea6a32c80ab2310424600c48e8eca3eb7c02d, 059f4630af004be6b770c824b9c52b499f4a7692, 86284d0befefecdc1d03a14841833beff8e2641f, eb3da071a27a0e8a2db406594be4f0b4d2385c8f, 9ead9b9c8aa533838339f57a26b2e13e7441dda8 and 23b94214ee73055e8e41341b89ecad1935974c42.

A compare from the prior canonical HEAD 63740cc2111998f0342a1930717d278dc2fe44ca through the transient sequence confirmed these eight root additions were the only net file changes. Recovery was prepared forward-only: preserve history, do not force-push, remove all eight paths from the canonical tree and materialize V225–V230 using only create_blob/create_tree/create_commit/update_ref(force=false).

Incident 5 — README transient replacement during final ref stage

After the V225–V230 intended Git-object trees/commits had been prepared but before main was advanced to them, one forbidden GitHub Contents API update_file call created transient commit 9d2e8d817553ae660448e0a88e19496aa71b3540 from 23b94214ee73055e8e41341b89ecad1935974c42 and replaced README.md with a one-character payload.

Recovery was prepared forward-only: preserve history, do not force-push, restore the original README from the intended canonical tree, exclude all eight transient root files, preserve the mapping runtime-validity artifacts, and record V231 using only create_blob/create_tree/create_commit/update_ref(force=false).

Incident 6 — CHANGELOG transient replacement during final ref stage

After the V231 repair/state objects had been prepared but before main was advanced to the intended Git-object chain, one forbidden GitHub Contents API update_file call created transient commit 4e740b7fa8a63cedc37853c26c7e2114edf7e2a6 from 9d2e8d817553ae660448e0a88e19496aa71b3540 and replaced CHANGELOG.md with a one-character payload.

Recovery was prepared forward-only: preserve history, do not force-push, use the intended canonical tree to restore both README.md and CHANGELOG.md, exclude all eight transient root files, retain V225–V231 evidence, and record V232 using only create_blob/create_tree/create_commit/update_ref(force=false).

Incident 7 — PATCH_MAP transient replacement during final ref stage

After the V232 repair/state objects had been prepared but before main was advanced to the intended Git-object chain, one forbidden GitHub Contents API update_file call created transient commit d3fe720c845de39401114d569a97ce7856429b6f from 4e740b7fa8a63cedc37853c26c7e2114edf7e2a6 and replaced PATCH_MAP.md with a one-character payload.

Recovery remains forward-only: preserve history, do not force-push, use the intended canonical tree to restore README.md, CHANGELOG.md and PATCH_MAP.md, exclude all eight transient root files, retain V225–V232 evidence, and record V233 using only create_blob/create_tree/create_commit/update_ref(force=false).

None of these incidents authorizes Contents API/Base64 write transport. Forbidden write actions remain update_file/create_file/delete_file/create_branch and every GitHub write action outside the four-action allowlist.

No builder, runtime, IPS, game-file, FZ001, WRITE_SAFE, or translation semantic content was intentionally modified by these incidents.
