MAPPING 10,036 HELPER TEXT V2 STATIC WRITE SAFETY AUTHORIZATION

Date: 2026-09-14 (KST)
Status: CANONICAL STATIC WRITE AUTHORIZATION / V2 NOT IMPLEMENTED
Scope: MAPPING_10036_HELPER_TEXT_V2_STATIC_WRITE_SAFETY_AUTHORIZATION_READ_ONLY
Basis HEAD: ad3c5edd9ca9d617acadfbd35b25712df983eba4
Semantic realization family: MAPPING_KOREAN_MISS_FALLBACK_COMPACT_V1

요약

The historical MAP10036_HELPER_TEXT_V1 evidence remains valid, but its exact 308-byte payload was not materialized and cannot be reproduced from preserved provenance. A new deterministic 308-byte helper is therefore assigned MAP10036_HELPER_TEXT_V2. V2 preserves the closed owner, hook, re-entry, ABI, mapping, capacity and guard contracts while receiving its own exact payload identity. Only the helper action identity changes; the forward hook V1, reverse hook V1 and delta V1 remain unchanged.

확정된 사실

- Canonical Switch main identity and Build ID remain unchanged.
- MAP10036_HELPER_TEXT_V2 occupies the same mapped 0x58CE60..0x58CF94 interval as historical V1: 308 bytes inside the validated 416-byte RX owner, leaving 108 bytes slack.
- The exact helper preimage remains the canonical structural-zero guard SHA-256 9575b2125169377b2ade7b401ea36c81228331d971f49664d9648d4f255d4868.
- V2 exact payload SHA-256 is f472326cab461ac8258282d5795031c33bcc52c9d2f1cd821b8eb6c94352f493.
- Deterministic source SHA-256 is 99d506ecf08b8b6628cb284e3124e196c28fada5cd061b11cf5f866121c79fb5 and linker-script SHA-256 is defb34c8e51067f2073bb9dba0703ec21a01d2815de0010894a8c5e7c061a275.
- Exact toolchain remains clang/LLD 17.0.0 revision 10999b6d034fe318f3d56c83bddb6572593a8bb0. Rebuild identity is byte-for-byte stable.
- Forward helper entry, reverse helper entry, original re-entry paths, LR behavior and live-register contract remain identical to the closed V237 contract.
- V2 uses only the already-approved helper storage owner and does not expand any write interval.
- The helper interval remains pairwise disjoint from MAP10036_FWD_MISS_HOOK_V1, MAP10036_REV_MISS_HOOK_V1 and MAP10036_DELTA_RODATA_V1.
- Exhaustive 16-bit verification remains exactly 2,542 forward hits and 2,542 reverse hits, zero extra hits, zero missing hits and exact 2,542/2,542 round-trip in both directions.
- Effective four-action diagnostic family becomes FWD_HOOK_V1 + REV_HOOK_V1 + HELPER_TEXT_V2 + DELTA_RODATA_V1. V1 is historical evidence and is not counted as an additional effective action.
- Existing F1 static WRITE_SAFE remains 158 and effective mapping WRITE_SAFE remains 4, so the explicit independently authorized total remains 162.
- MAP10036_HELPER_TEXT_V2 receives write_authority = WRITE_SAFE and action_status = STATIC_WRITE_AUTHORIZED_NOT_IMPLEMENTED. Runtime validation remains mandatory.

유력한 가설

A delivery-controlled diagnostic build containing the three unchanged V1 actions plus HELPER_TEXT_V2 is sufficient to test the remaining runtime mapping hypothesis.

미확정 사항

- Actual runtime execution on target hardware.
- Visible forward/reverse Korean mapping behavior after delivery.
- Release-level integration with unrelated runtime families.

기각된 가설

- Reusing the historical V1 authorization for a byte-different helper without a new action identity.
- Rewriting historical V1 payload identity to the V2 hash.
- Reopening the mapping semantic design, hook owner, storage search or translation decisions.
- Counting V1 and V2 as two simultaneous effective mapping actions.

관련 영향 범위

Only the helper payload identity and effective helper action ID change. FZ001, Stage1, Stage2, F1, forward-986, pointer-56, inline accounting, translations, other descriptor/font/yomi families and the other three mapping actions remain unchanged.

수정 제안

Proceed to MAPPING_10036_DELIVERY_CONTROLLED_DIAGNOSTIC_BUILD with exactly four effective actions and fail closed on any input, guard, payload, coordinate, action-set or IPS round-trip mismatch. Binary payload transport through GitHub remains unauthorized; deterministic regeneration is the authorized implementation route.
